language: cpp
os:
  - linux
  - osx
compiler:
  - gcc
  - clang
# Change this to your needs
before_install:
  - if [ "$TRAVIS_OS_NAME" == "linux" ]; then sudo apt-get update -qq; sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test; fi
install:
  - if [ "$TRAVIS_OS_NAME" == "linux" ]; then sudo apt-get install -qq gcc-4.8; sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-4.8 50; fi
  - if [ "$TRAVIS_OS_NAME" == "linux" ]; then sudo apt-get install -qq g++-4.8; sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-4.8 50; fi
script: $CC --version && $CXX --version && make
matrix:
  allow_failures:
    - os: osx